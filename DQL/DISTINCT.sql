--Remove duplicates each value appears only once
--Syntax
SELECT DISTINCT columnName
FROM TableName
--Don't user DISTINCT unless it's necessary, it can slow down your query

--Return unique list of all countries
SELECT DISTINCT country
FROM customers


